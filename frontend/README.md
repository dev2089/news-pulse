# Frontend

The dashboard lives in frontend/dashboard.jsx and is mounted by the root Next.js route at app/page.jsx.

The visual system is intentionally neumorphic: a pale shared surface uses opposing soft shadows for raised controls, while inset shadows communicate filters, metrics and timeline tracks.
